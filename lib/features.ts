// Feature flags for nook-business.

// Self-serve cafe claiming: an owner searches for their cafe, gets a
// verification code, and sends it from the cafe's official Instagram; a
// superadmin then manually approves the claim (in nook-admin). This is the
// intended onboarding process, so it is ON.
export const SELF_SERVE_CLAIM_ENABLED = true
